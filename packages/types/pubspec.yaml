name: types
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  backend:
    path: ../backend

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
